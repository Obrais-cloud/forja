# ollaflow

| field | value |
|---|---|
| kind | `tool` |
| path | `/Users/braisrevalderia/ollaflow` |
| github | — |
| version | — |
| created | 2026-05-28 |
| tags | ollama, pipeline, workflow, yaml |

**What it is.** Pipelines composables de modelos para Ollama. Encadena modelos por el fleet con YAML.

**When to reach for it.** Para multi-paso (extract → reason → reformat) usando distintos modelos por paso, declarado en YAML.

## Notes

*(free-form — anything you want to remember; `forja sync` won't overwrite this section.)*
