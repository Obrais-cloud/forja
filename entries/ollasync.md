# ollasync

| field | value |
|---|---|
| kind | `tool` |
| path | `/Users/braisrevalderia/ollasync` |
| github | — |
| version | — |
| created | 2026-05-28 |
| tags | ollama, fleet, sync, yaml |

**What it is.** Sincronización declarativa de modelos. YAML que define qué modelos viven en qué nodos, y `ollasync` los sincroniza.

**When to reach for it.** Para mantener consistencia de inventario de modelos entre nodos sin pulls manuales en cada uno.

## Notes

*(free-form — anything you want to remember; `forja sync` won't overwrite this section.)*
